---
import { Container } from '@/components/container';
import Layout from '@/layouts/layout.astro';
import { x as designers } from '@/data/designers';
import Breadcrumbs from '@/components/breadcrumbs.astro';
---

<Layout
  description={`Browse all ${designers.length} exceptional designers curated by DesEngs and find new creative practitioners to follow.`}
  title="All Curated Designers — DSGNRS"
>
  <Container>
    <header class="header">
      <a href="/dsgnrs">[back]</a>
    </header>

    <Breadcrumbs
      items={[
        { label: 'Home', url: '/' },
        { label: 'DSGNRS', url: '/dsgnrs' },
        { label: 'All Designers' },
      ]}
    />

    <section class="designers">
      {
        designers.map(designer => (
          <a
            href={`https://x.com/${designer.username}`}
            rel="noreferrer"
            target="_blank"
          >
            @{designer.username}
          </a>
        ))
      }
    </section>
  </Container>
</Layout>

<style>
  .header {
    padding: 60px 0 30px;

    & a {
      font-family: var(--font-mono);
      font-size: var(--font-2xsm);
      font-weight: 500;
      color: var(--color-foreground-subtler);
      text-decoration: none;
      text-transform: uppercase;
    }
  }

  .designers {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    padding-bottom: 60px;

    & a {
      font-family: var(--font-mono);
      font-size: var(--font-2xsm);
      font-weight: 500;
      color: var(--color-foreground);
      text-decoration: none;
      text-transform: uppercase;
    }
  }
</style>
